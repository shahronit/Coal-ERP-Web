rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All ERP data access goes through Cloud Run Express API (Admin SDK).
    // Client apps must not read/write Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
